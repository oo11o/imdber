import gethtml from '../src/gethtml.js';

const url = 'https://www.imdb.com/title/tt0088247/';
// const url = 'https://psymost.ru/'

// test('get html and status', () => {
//     gethtml(url).then((data) => {
//             expect(data.status).toEqual(200);
//             expect(data.data.includes('"name":"The Terminator"')).toEqual(true);
//         })
// });

test('', () => {

});
